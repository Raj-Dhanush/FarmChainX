// This file can be replaced during build by using the `fileReplacements` array.
// However, we're using dynamic import to handle both dev and production modes.

export const environment = {
    production: false,
    apiUrl: 'http://localhost:8080', // Local development backend
    appName: 'FarmChainX',
    version: '1.0.0'
};
